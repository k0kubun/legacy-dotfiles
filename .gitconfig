[user]
    name  = ikstrm
    email = mail@ikst.jp
[color]
    ui = true
[core]
    excludesfile = ~/.gitignore
[alias]
    co = checkout
    st = status -sb
    pr = pull --rebase
    fo = fetch origin
    ro = rebase origin
    rc = rebase --continue
    wd = diff --word-diff
    gp = grep -n
    lg = log --graph --pretty=oneline --decorate --date=short --abbrev-commit --branches