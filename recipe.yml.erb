resources:
  <%- homebrew_package_names.each do |homebrew_package_name| -%>
  - type: homebrew_package
    name: <%= homebrew_package_name %>
  <%- end -%>
  - type: homebrew_package
    name: vim
    options: --with-lua --with-luajit
  - type: homebrew_package
    name: go
    options: --with-cc-common
